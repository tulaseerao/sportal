 <div class="span3">
    <div class="well sidebar-nav">
      <ul class="nav nav-list">
        <li class="nav-header">Summary</li>
        <li  ng-class="{'active': dept.name==='English'}" ng-click="active_class($event)" ng-repeat="dept in depts">
          <a href="#">{{dept.name}}</a>
        </li>
      </ul>
    </div><!--/.well -->
</div><!--/span-->
